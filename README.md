See Overview.md for the format.

The /tasks are intended to be copied into a project in .rune-spiral/tasks.

The user is free to edit them, and create their own templates in that folder.

When a task is *akshually* run, it is copied from the template into a /tasks folder in the app root.